<div class="candy-tape" aria-hidden="true">
  <div class="candy-tape__track">
    <span class="candy-tape__segment candy-tape__segment--left"></span>
    <span class="candy-tape__segment candy-tape__segment--main"></span>
    <span class="candy-tape__segment candy-tape__segment--right"></span>
  </div>
</div>

<style>
  .candy-tape {
    position: relative;
    z-index: 6;
    display: grid;
    align-items: center;
    width: 100%;
    min-height: clamp(4.5rem, 9vw, 6.5rem);
    margin-block: -1px;
    padding: 0 clamp(1rem, 4vw, 3rem);
    overflow: hidden;
    background:
      radial-gradient(ellipse 42% 100% at 50% 100%, rgba(255, 78, 205, 0.12), transparent 70%),
      linear-gradient(180deg, #070b14 0%, #0b0a19 42%, #140820 100%);
  }

  .candy-tape::before {
    content: '';
    position: absolute;
    inset: 50% 0 auto;
    height: 1px;
    background: linear-gradient(
      90deg,
      transparent,
      rgba(255, 255, 255, 0.08) 15%,
      rgba(255, 255, 255, 0.08) 85%,
      transparent
    );
  }

  .candy-tape__track {
    position: relative;
    display: grid;
    grid-template-columns: minmax(4rem, 0.75fr) minmax(12rem, 2fr) minmax(4rem, 0.75fr);
    gap: clamp(0.65rem, 2.4vw, 2rem);
    align-items: center;
    width: min(100%, 74rem);
    margin: 0 auto;
  }

  .candy-tape__segment {
    display: block;
    height: clamp(1.35rem, 2.8vw, 1.8rem);
    filter: drop-shadow(0 0 14px rgba(255, 78, 205, 0.18));
  }

  .candy-tape__segment--left {
    background: linear-gradient(90deg, #5de4ff, #10b981);
    clip-path: polygon(0 0, 76% 0, 100% 100%, 0 100%);
  }

  .candy-tape__segment--main {
    background: linear-gradient(90deg, #ff4ecd 0%, #ff6fbd 55%, #ffd166 100%);
    clip-path: polygon(0 0, 92% 0, 100% 100%, 8% 100%);
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.3),
      0 0 24px rgba(255, 78, 205, 0.16);
  }

  .candy-tape__segment--right {
    background: linear-gradient(90deg, #ffd166, #d2ff00);
    clip-path: polygon(0 0, 100% 0, 100% 100%, 24% 100%);
  }

  @media (max-width: 560px) {
    .candy-tape {
      min-height: 3.75rem;
    }

    .candy-tape__track {
      grid-template-columns: minmax(2.25rem, 0.45fr) minmax(8rem, 1.7fr) minmax(2.25rem, 0.45fr);
      gap: 0.5rem;
    }

    .candy-tape__segment {
      height: 1.2rem;
    }
  }
</style>
